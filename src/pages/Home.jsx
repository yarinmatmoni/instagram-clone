import { Header } from '../components/index';
import { storyService } from '../services/story.service';

const Home = () => {
	return (
		<div>
			<Header />
			<div>Home</div>
		</div>
	);
};

export default Home;
